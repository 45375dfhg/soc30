android {
            defaultConfig {
                applicationId = "org.nativescript.nestedroutertabview"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }